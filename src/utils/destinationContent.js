export const getDestinationWhyVisit = (destination) =>
  `${destination.description} Visit for ${destination.highlights.slice(0, 3).join(", ").toLowerCase()}, guided day-wise planning, and a travel experience that balances comfort with memorable local moments.`;
